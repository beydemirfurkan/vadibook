MEILI_HOST=http://localhost:7700
# search-only key (see deploy/README.md); never the master key
MEILI_SEARCH_KEY=
VADIBOOK_SQLITE=../data/private/vadibook.sqlite
# shown in the header when set
NEXT_PUBLIC_REPO_URL=
# public origin, used for absolute Open Graph URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
