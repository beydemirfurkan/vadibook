# Optional: move all pipeline data elsewhere (default: <repo>/data)
# VADIBOOK_DATA=D:\vadibook-data
# No Hugging Face token is needed: diarization models are downloaded from GitHub releases on first use.
